sudo: required
services:
  - docker
before_install:
  - docker build -t tadamczyk/docker-backend -f docker/backend/Dockerfile.dev backend/.
  - docker build -t tadamczyk/docker-frontend -f docker/frontend/Dockerfile.dev frontend/.
script:
  - docker run -e CI=true tadamczyk/docker-frontend npm test